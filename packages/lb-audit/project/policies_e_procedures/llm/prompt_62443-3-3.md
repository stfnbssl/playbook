```txt
Genera un database JSON conforme allo schema fornito, creando template per policy e procedure per la compliance con IEC 62443-3-3, basati sul requisito SR specificato nel JSON di input. I template devono essere pertinenti al contesto del Product Supplier fornito, con le seguenti specifiche:

### Obiettivo
- Creare template dettagliati per policy e procedure che soddisfino il requisito SR (es. SR 6.2) e siano personalizzati per le macchine, i protocolli e le applicazioni del Product Supplier (Breton Spa).
- Garantire che i template siano pratici, con esempi specifici per almeno una macchina tipo (es. CNC Machining Centers) e riferimenti ai requisiti di sicurezza (CR) supportati (es. CR3, CR5).

### Contesto del Product Supplier
- Utilizza il JSON <product_supplier_context> per personalizzare i template, mappando ogni policy statement e procedure step alle key_features (es. Firmware Signing, Network Segmentation) e riflettendo la criticità delle applicazioni (es. alta per Natural Stone Processing).
- Considera l’ambiente operativo (es. high dust levels, temperature 5-40°C) e i protocolli supportati (es. OPC UA, PROFINET) per garantire la fattibilità delle procedure.
- Priorizza i `challenges_addressed` in base ai `common_threats` (es. ransomware, MitM attacks) e suggerisci strumenti compatibili con i protocolli, includendo opzioni open-source e proprietarie.

### Requisiti Tecnici
- **Policy Template**:
  - Includi `purpose`, `scope`, `policy_statements`, `approval`, e `verification_checklist`.
  - Ogni `policy_statement` deve essere mappato a una key_feature o CR (es. CR3: System Integrity) e includere esempi specifici per il contesto (es. monitoraggio di OPC UA su CNC Machining Centers).
  - La `verification_checklist` deve includere controlli quantificabili per ciascun SL-C supportato (SL 2, SL 3, SL 4) e riflettere le certificazioni IEC 62443-4-1 e 4-2.
- **Procedure Template**:
  - Includi `purpose`, `scope`, `prerequisites`, `procedure_steps`, `tools_required`, `challenges_addressed`, e `verification_checklist`.
  - Ogni `procedure_step` deve essere mappato a una key_feature o CR e includere dettagli operativi (es. configurazione di SIEM per PROFINET).
  - I `tools_required` devono suggerire strumenti specifici (es. Siemens SINUMERIK, Splunk) con alternative open-source (es. Wireshark).
- **Placeholders**: Specifica il tipo di input atteso per i placeholders (es. nome del ruolo, versione del firmware) e includi valori predefiniti suggeriti basati sul contesto (es. ‘version 2025.1’ per il firmware).

### Dettagli di Implementazione
- Usa il JSON <input_json_data> per popolare il campo `id` e allineare i template al requisito SR specificato (es. SR 6.2).
- Verifica l’allineamento con i livelli di sicurezza (SL-C 2, 3, 4) e le certificazioni del Product Supplier.
- Fornisci un JSON risultante chiaro, con esempi pratici e controlli quantificabili per la verifica.

### Schema JSON
Popola il seguente schema JSON: <database_json_schema>

Ritorna il JSON risultante, garantendo che i template siano concisi, pratici e direttamente applicabili al contesto di Breton Spa.
```
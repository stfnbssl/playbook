ai lab
    api-url http://localhost:3000
    call clist-item-2.1.1
        description clist extension of item 2.1.1
        llm_service deepSeek
        llm_model deepseek-chat
        max_tokens 8192
        temperature 0.7
        message 
            role system
            content Sei un esperto in cybersecurity industriale contesto OT / ICS,
                \ed assisti gli auditor nel produrre checklist.
        message 
            role user
            content Ho necessità di popolare un database JSON con dati utili per supportare l'uso di una check list.
                \n Sotto riporto il JSON schema del database e il JSON di un item della check list.
                \n Popola lo schema con l'idd fornito nel JSON originale e aggiungi:
                \n 1) la proprieta `asset_owner` come da schema con la valutazione motivata che il controllo è rilevante per una verifica di un IACS gestito da un asset owner.
                \n 2) la proprieta `ssdl` come da schema con la valutazione motivata che il controllo è rilevante per una verifica del Secure Software Development Lifecycle
                \n 3) la proprieta `system_integration` come da schema con la valutazione motivata che il controllo è rilevante per la verifica di un IACS System o macchina
                \n 4) la proprieta `component_production` come da schema con la valutazione motivata che il controllo è rilevante per la verifica di un componente prodotto
                \n - Se il JSON originale manca di dettagli, deduci informazioni plausibili basandoti sulla norma.
                \n - Verifica che il JSON risultante sia conforme allo schema e privo di errori.
                \n - Se hai note significative sul JSON risultante puoi popolare la proprietà `note_su_json_risultante`.
                \n Popola questo JSON schema:
                \n <database_json_schema> 
                \n {"$schema":"http://json-schema.org/draft-07/schema#","title":"Schema per check list Carel Logbot Vendor Assessment","description":"Schema JSON per raccogliere classificazioni degli item della checklist.","type":"object","properties":{"IDD":{"type":"string"},"area":{"type":"string"},"category":{"type":"string"},"description":{"type":"string"},"asset_owner":{"type":"object","properties":{"isRelevant":{"type":"boolean"},"motivation":{"type":"string"}}},"ssdl":{"type":"object","properties":{"isRelevant":{"type":"boolean"},"motivation":{"type":"string"}}},"system_integration":{"type":"object","properties":{"isRelevant":{"type":"boolean"},"motivation":{"type":"string"}}},"component_production":{"type":"object","properties":{"isRelevant":{"type":"boolean"},"motivation":{"type":"string"}}}},"required":["ssdl","system_integration","component_production"],"additionalProperties":false} 
                \n </database_json_schema> 
                \n Popola lo schema con i seguenti dati di input
                \n <input_json_data> 
                \n {"idd":"2.1.1","area":"Continuità operativa","category":"People","description":"La continuità dell'erogazione dei servizi di fornitura è garantita anche in situazioni di emergenza (p.e. indisponibilità del personale, guasti tecnici)?","note":""} 
                \n </input_json_data> 
                \n Ritorna il JSON risultante.
    call clist-item-2.1.2
        description clist extension of item 2.1.2
        llm_service deepSeek
        llm_model deepseek-chat
        max_tokens 8192
        temperature 0.7
        message 
            role system
            content Sei un esperto in cybersecurity industriale contesto OT / ICS,
                \ed assisti gli auditor nel produrre checklist.
        message 
            role user
            content Ho necessità di popolare un database JSON con dati utili per supportare l'uso di una check list.
                \n Sotto riporto il JSON schema del database e il JSON di un item della check list.
                \n Popola lo schema con l'idd fornito nel JSON originale e aggiungi:
                \n 1) la proprieta `asset_owner` come da schema con la valutazione motivata che il controllo è rilevante per una verifica di un IACS gestito da un asset owner.
                \n 2) la proprieta `ssdl` come da schema con la valutazione motivata che il controllo è rilevante per una verifica del Secure Software Development Lifecycle
                \n 3) la proprieta `system_integration` come da schema con la valutazione motivata che il controllo è rilevante per la verifica di un IACS System o macchina
                \n 4) la proprieta `component_production` come da schema con la valutazione motivata che il controllo è rilevante per la verifica di un componente prodotto
                \n - Se il JSON originale manca di dettagli, deduci informazioni plausibili basandoti sulla norma.
                \n - Verifica che il JSON risultante sia conforme allo schema e privo di errori.
                \n - Se hai note significative sul JSON risultante puoi popolare la proprietà `note_su_json_risultante`.
                \n Popola questo JSON schema:
                \n <database_json_schema> 
                \n {"$schema":"http://json-schema.org/draft-07/schema#","title":"Schema per check list Carel Logbot Vendor Assessment","description":"Schema JSON per raccogliere classificazioni degli item della checklist.","type":"object","properties":{"IDD":{"type":"string"},"area":{"type":"string"},"category":{"type":"string"},"description":{"type":"string"},"asset_owner":{"type":"object","properties":{"isRelevant":{"type":"boolean"},"motivation":{"type":"string"}}},"ssdl":{"type":"object","properties":{"isRelevant":{"type":"boolean"},"motivation":{"type":"string"}}},"system_integration":{"type":"object","properties":{"isRelevant":{"type":"boolean"},"motivation":{"type":"string"}}},"component_production":{"type":"object","properties":{"isRelevant":{"type":"boolean"},"motivation":{"type":"string"}}}},"required":["ssdl","system_integration","component_production"],"additionalProperties":false} 
                \n </database_json_schema> 
                \n Popola lo schema con i seguenti dati di input
                \n <input_json_data> 
                \n {"idd":"2.1.2","area":"Continuità operativa","category":"People","description":"In caso di indisponibilità delle persone chiave per l'erogazione dei servizi di fornitura, è stato individuato un elenco di specialisti qualificati a supporto e sono definiti piani di \"call-up\" per il contatto tempestivo di queste figure?","note":""} 
                \n </input_json_data> 
                \n Ritorna il JSON risultante.
    call clist-item-2.1.3
        description clist extension of item 2.1.3
        llm_service deepSeek
        llm_model deepseek-chat
        max_tokens 8192
        temperature 0.7
        message 
            role system
            content Sei un esperto in cybersecurity industriale contesto OT / ICS,
                \ed assisti gli auditor nel produrre checklist.
        message 
            role user
            content Ho necessità di popolare un database JSON con dati utili per supportare l'uso di una check list.
                \n Sotto riporto il JSON schema del database e il JSON di un item della check list.
                \n Popola lo schema con l'idd fornito nel JSON originale e aggiungi:
                \n 1) la proprieta `asset_owner` come da schema con la valutazione motivata che il controllo è rilevante per una verifica di un IACS gestito da un asset owner.
                \n 2) la proprieta `ssdl` come da schema con la valutazione motivata che il controllo è rilevante per una verifica del Secure Software Development Lifecycle
                \n 3) la proprieta `system_integration` come da schema con la valutazione motivata che il controllo è rilevante per la verifica di un IACS System o macchina
                \n 4) la proprieta `component_production` come da schema con la valutazione motivata che il controllo è rilevante per la verifica di un componente prodotto
                \n - Se il JSON originale manca di dettagli, deduci informazioni plausibili basandoti sulla norma.
                \n - Verifica che il JSON risultante sia conforme allo schema e privo di errori.
                \n - Se hai note significative sul JSON risultante puoi popolare la proprietà `note_su_json_risultante`.
                \n Popola questo JSON schema:
                \n <database_json_schema> 
                \n {"$schema":"http://json-schema.org/draft-07/schema#","title":"Schema per check list Carel Logbot Vendor Assessment","description":"Schema JSON per raccogliere classificazioni degli item della checklist.","type":"object","properties":{"IDD":{"type":"string"},"area":{"type":"string"},"category":{"type":"string"},"description":{"type":"string"},"asset_owner":{"type":"object","properties":{"isRelevant":{"type":"boolean"},"motivation":{"type":"string"}}},"ssdl":{"type":"object","properties":{"isRelevant":{"type":"boolean"},"motivation":{"type":"string"}}},"system_integration":{"type":"object","properties":{"isRelevant":{"type":"boolean"},"motivation":{"type":"string"}}},"component_production":{"type":"object","properties":{"isRelevant":{"type":"boolean"},"motivation":{"type":"string"}}}},"required":["ssdl","system_integration","component_production"],"additionalProperties":false} 
                \n </database_json_schema> 
                \n Popola lo schema con i seguenti dati di input
                \n <input_json_data> 
                \n {"idd":"2.1.3","area":"Continuità operativa","category":"People","description":"Il personale a supporto dell'erogazione dei servizi di fornitura è distribuito su sedi diversificate e/o sono presenti procedure per gestire la distribuzione del personale in siti alternativi in situazioni di crisi o emergenza (p.e. ri-distribuzione del personale, remote working)?","note":""} 
                \n </input_json_data> 
                \n Ritorna il JSON risultante.
    call clist-item-2.2.1
        description clist extension of item 2.2.1
        llm_service deepSeek
        llm_model deepseek-chat
        max_tokens 8192
        temperature 0.7
        message 
            role system
            content Sei un esperto in cybersecurity industriale contesto OT / ICS,
                \ed assisti gli auditor nel produrre checklist.
        message 
            role user
            content Ho necessità di popolare un database JSON con dati utili per supportare l'uso di una check list.
                \n Sotto riporto il JSON schema del database e il JSON di un item della check list.
                \n Popola lo schema con l'idd fornito nel JSON originale e aggiungi:
                \n 1) la proprieta `asset_owner` come da schema con la valutazione motivata che il controllo è rilevante per una verifica di un IACS gestito da un asset owner.
                \n 2) la proprieta `ssdl` come da schema con la valutazione motivata che il controllo è rilevante per una verifica del Secure Software Development Lifecycle
                \n 3) la proprieta `system_integration` come da schema con la valutazione motivata che il controllo è rilevante per la verifica di un IACS System o macchina
                \n 4) la proprieta `component_production` come da schema con la valutazione motivata che il controllo è rilevante per la verifica di un componente prodotto
                \n - Se il JSON originale manca di dettagli, deduci informazioni plausibili basandoti sulla norma.
                \n - Verifica che il JSON risultante sia conforme allo schema e privo di errori.
                \n - Se hai note significative sul JSON risultante puoi popolare la proprietà `note_su_json_risultante`.
                \n Popola questo JSON schema:
                \n <database_json_schema> 
                \n {"$schema":"http://json-schema.org/draft-07/schema#","title":"Schema per check list Carel Logbot Vendor Assessment","description":"Schema JSON per raccogliere classificazioni degli item della checklist.","type":"object","properties":{"IDD":{"type":"string"},"area":{"type":"string"},"category":{"type":"string"},"description":{"type":"string"},"asset_owner":{"type":"object","properties":{"isRelevant":{"type":"boolean"},"motivation":{"type":"string"}}},"ssdl":{"type":"object","properties":{"isRelevant":{"type":"boolean"},"motivation":{"type":"string"}}},"system_integration":{"type":"object","properties":{"isRelevant":{"type":"boolean"},"motivation":{"type":"string"}}},"component_production":{"type":"object","properties":{"isRelevant":{"type":"boolean"},"motivation":{"type":"string"}}}},"required":["ssdl","system_integration","component_production"],"additionalProperties":false} 
                \n </database_json_schema> 
                \n Popola lo schema con i seguenti dati di input
                \n <input_json_data> 
                \n {"idd":"2.2.1","area":"Continuità operativa","category":"Physical infrastructure","description":"Le sedi dell'organizzazione sono dotate di adeguati presidi di sicurezza, sottoposte a manutenzione periodica contro eventi di carattere ambientale? Sono previsti piani di emergenza per gestire i disastri ambientali che potrebbero coinvolgere tali edifici?","note":""} 
                \n </input_json_data> 
                \n Ritorna il JSON risultante.
    call clist-item-2.2.2
        description clist extension of item 2.2.2
        llm_service deepSeek
        llm_model deepseek-chat
        max_tokens 8192
        temperature 0.7
        message 
            role system
            content Sei un esperto in cybersecurity industriale contesto OT / ICS,
                \ed assisti gli auditor nel produrre checklist.
        message 
            role user
            content Ho necessità di popolare un database JSON con dati utili per supportare l'uso di una check list.
                \n Sotto riporto il JSON schema del database e il JSON di un item della check list.
                \n Popola lo schema con l'idd fornito nel JSON originale e aggiungi:
                \n 1) la proprieta `asset_owner` come da schema con la valutazione motivata che il controllo è rilevante per una verifica di un IACS gestito da un asset owner.
                \n 2) la proprieta `ssdl` come da schema con la valutazione motivata che il controllo è rilevante per una verifica del Secure Software Development Lifecycle
                \n 3) la proprieta `system_integration` come da schema con la valutazione motivata che il controllo è rilevante per la verifica di un IACS System o macchina
                \n 4) la proprieta `component_production` come da schema con la valutazione motivata che il controllo è rilevante per la verifica di un componente prodotto
                \n - Se il JSON originale manca di dettagli, deduci informazioni plausibili basandoti sulla norma.
                \n - Verifica che il JSON risultante sia conforme allo schema e privo di errori.
                \n - Se hai note significative sul JSON risultante puoi popolare la proprietà `note_su_json_risultante`.
                \n Popola questo JSON schema:
                \n <database_json_schema> 
                \n {"$schema":"http://json-schema.org/draft-07/schema#","title":"Schema per check list Carel Logbot Vendor Assessment","description":"Schema JSON per raccogliere classificazioni degli item della checklist.","type":"object","properties":{"IDD":{"type":"string"},"area":{"type":"string"},"category":{"type":"string"},"description":{"type":"string"},"asset_owner":{"type":"object","properties":{"isRelevant":{"type":"boolean"},"motivation":{"type":"string"}}},"ssdl":{"type":"object","properties":{"isRelevant":{"type":"boolean"},"motivation":{"type":"string"}}},"system_integration":{"type":"object","properties":{"isRelevant":{"type":"boolean"},"motivation":{"type":"string"}}},"component_production":{"type":"object","properties":{"isRelevant":{"type":"boolean"},"motivation":{"type":"string"}}}},"required":["ssdl","system_integration","component_production"],"additionalProperties":false} 
                \n </database_json_schema> 
                \n Popola lo schema con i seguenti dati di input
                \n <input_json_data> 
                \n {"idd":"2.2.2","area":"Continuità operativa","category":"Physical infrastructure","description":"Sono presenti misure di sicurezza volte a controllare e limitare gli accessi fisici alle sedi dell'organizzazione (p.e. portineria, videosorveglianza, lettori badge)?","note":""} 
                \n </input_json_data> 
                \n Ritorna il JSON risultante.
    call clist-item-2.3.1
        description clist extension of item 2.3.1
        llm_service deepSeek
        llm_model deepseek-chat
        max_tokens 8192
        temperature 0.7
        message 
            role system
            content Sei un esperto in cybersecurity industriale contesto OT / ICS,
                \ed assisti gli auditor nel produrre checklist.
        message 
            role user
            content Ho necessità di popolare un database JSON con dati utili per supportare l'uso di una check list.
                \n Sotto riporto il JSON schema del database e il JSON di un item della check list.
                \n Popola lo schema con l'idd fornito nel JSON originale e aggiungi:
                \n 1) la proprieta `asset_owner` come da schema con la valutazione motivata che il controllo è rilevante per una verifica di un IACS gestito da un asset owner.
                \n 2) la proprieta `ssdl` come da schema con la valutazione motivata che il controllo è rilevante per una verifica del Secure Software Development Lifecycle
                \n 3) la proprieta `system_integration` come da schema con la valutazione motivata che il controllo è rilevante per la verifica di un IACS System o macchina
                \n 4) la proprieta `component_production` come da schema con la valutazione motivata che il controllo è rilevante per la verifica di un componente prodotto
                \n - Se il JSON originale manca di dettagli, deduci informazioni plausibili basandoti sulla norma.
                \n - Verifica che il JSON risultante sia conforme allo schema e privo di errori.
                \n - Se hai note significative sul JSON risultante puoi popolare la proprietà `note_su_json_risultante`.
                \n Popola questo JSON schema:
                \n <database_json_schema> 
                \n {"$schema":"http://json-schema.org/draft-07/schema#","title":"Schema per check list Carel Logbot Vendor Assessment","description":"Schema JSON per raccogliere classificazioni degli item della checklist.","type":"object","properties":{"IDD":{"type":"string"},"area":{"type":"string"},"category":{"type":"string"},"description":{"type":"string"},"asset_owner":{"type":"object","properties":{"isRelevant":{"type":"boolean"},"motivation":{"type":"string"}}},"ssdl":{"type":"object","properties":{"isRelevant":{"type":"boolean"},"motivation":{"type":"string"}}},"system_integration":{"type":"object","properties":{"isRelevant":{"type":"boolean"},"motivation":{"type":"string"}}},"component_production":{"type":"object","properties":{"isRelevant":{"type":"boolean"},"motivation":{"type":"string"}}}},"required":["ssdl","system_integration","component_production"],"additionalProperties":false} 
                \n </database_json_schema> 
                \n Popola lo schema con i seguenti dati di input
                \n <input_json_data> 
                \n {"idd":"2.3.1","area":"Continuità operativa","category":"Information and data","description":"È presente una procedura aziendale per classificare, etichettare e conservare le informazioni sia cartacee che elettroniche?","note":""} 
                \n </input_json_data> 
                \n Ritorna il JSON risultante.